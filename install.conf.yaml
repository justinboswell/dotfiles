- defaults:
    link:
      relink: true

- shell:
  - [git submodule update --init --recursive, Installing submodules]

- clean: ['~']

- link:
    ~/.config: config
    ~/.gitconfig: gitconfig
    ~/.zshrc: zshrc

