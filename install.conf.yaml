- defaults:
  	link:
      	relink: true

- clean: ['~']

- link:
	~/.config: config
	~/.gitconfig: gitconfig
	~/.zshrc: zshrc

- shell:
    - [git submodule update --init --recursive, Installing submodules]
